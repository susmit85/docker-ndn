
```
docker exec -i -t docker-ndn bash
```


This repo contains a recipe for making a [Docker](http://docker.io) container for NDN. 
To build, make sure you have Docker [installed](http://www.docker.io/gettingstarted/), clone this repo somewhere, and then run:
```
docker build -rm -t ndn.

```
